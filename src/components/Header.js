import React from 'react';

const Header = () => {
    return (
        <header className="header">
            <h1 className="header-title">Virus Alert</h1>
            <p className="header-subtitle">Please enter the key to proceed</p>
        </header>
    );
};

export default Header;